import React from 'react';
import markerIcon from '../images/map-marker-2-xxl.png';

const Marker = () => {
  return (
    <img alt="marker" src={ markerIcon }>
    </img>
  );
};

export default Marker;
